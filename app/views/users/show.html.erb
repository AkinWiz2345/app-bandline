<div class='container px-4 py-4'>

  <div class="row">
    <aside class="col-sm-4">
      <div class="card">
        <div class="card-header">
          <h3 class="card-title"><%= @user.name %></h3>
        </div>
        <div class="card-body">
          <% if @user.image? %>
            <%= image_tag @user.image.url, class: "rounded img-fluid" %>
          <% end %>
        </div>
      </div>
    </aside>
    <div class="col-sm-8">
      <ul class="nav nav-tabs nav-justified mb-3">
        <li class="nav-item"><a href="#" class="nav-link">投稿記事</a></li>
        <li class="nav-item"><a href="#" class="nav-link">気になるリスト</a></li>
      </ul>
    </div>
  </div>

</div>